# P5 local operational implementation — 2026-09-06

Baseline 0428c1b; uncommitted continuation. G5 is NOT promoted because G4/full GS-SURFACE remains incomplete.

Implemented `sot engine --store ...` local import/promote/rollback/status/doctor plus explicit prepare/probe/sync/search/runtime-status. Runtime commands require trusted external operation registry, exact protocol and explicit private runtime root/generation. Search normalizes only the measured release table schema and invokes existing subject verification; unknown native index freshness stays unknown. No raw command proxy, download, PATH discovery or automatic default-provider changes. Registry filtering excludes unrelated artifact/provider/commit/protocol records; duplicate selected operations refuse rather than last-wins. Healthy probe exit semantics repaired after independent review.

Real signed-release E2E receipts in `evidence/continuation-native/`: `sot-lifecycle-e2e.json` preserves initial ensure_index abstention, `sot-lifecycle-explicit-index.json` proves repaired explicit provider index exit 0, `sot-verified-search.json` proves normalized greet source VERIFIED with unknown freshness. Fresh exact release compatibility capture PASS separately. `surface-snapshot.json` includes actual help and static MCP declarations; do not call static inventory behavioral harness discovery.

Tests: `reviewer-regressions-final.log`: 9 passed, exit 0. Initial test failure retained. Independent review found registry mixing/duplicate, probe exit, normalization and error-boundary issues; repaired with regression tests. Final independent reviewer agent_27db4726-74a7-4719-b013-c2959a829aeb disposition CLEAN, relayed by coordinator and recorded in `evidence/continuation-native/independent-review.json`. Full suite receipt will be under `final-suite.log`/`final-suite-exit.json`.

Update: safe logical uninstall now removes only verified selected owned pointer and preserves all payload/data. Managed sync now uses existing CodebaseMemoryProvider atomic ledger API under project WriteLock with a sync-only DB connection. Read search reuses existing assurance.orchestrator normalization/trust ceilings; no parallel trust path. Commit and protocol input normalization repaired with regressions. Known limits: no destructive data reclamation; no public persisted managed provider configuration/query default. Full SUR-01..12 platform/harness coexistence and upgrade-race coverage is not established. These are implementation/coverage gaps, not grounds to lower trust or certify support.
